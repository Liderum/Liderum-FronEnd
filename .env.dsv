# Ambiente: DSV (desenvolvimento local + Vercel Preview)
# Fallback para localhost quando as variáveis não estiverem definidas.
# Este arquivo PODE ser commitado — não contém secrets reais.

VITE_APP_ENV=dsv

VITE_AUTH_API_URL=http://localhost:5065/liderum/api/login
VITE_USERS_API_URL=http://localhost:5065/liderum/api/user
VITE_WORKS_API_URL=https://localhost:7141/api/v1
VITE_RBAC_API_URL=http://localhost:5065/liderum/api/rbac

# Descomente para ativar dados mockados (útil quando o backend não está disponível)
# VITE_USE_MOCK=true
