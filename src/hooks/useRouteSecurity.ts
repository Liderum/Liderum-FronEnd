/**
 * Segurança de sessão agora é gerenciada por tokens em memória e cookies HttpOnly.
 * Não há mais necessidade de limpar localStorage ao navegar entre rotas.
 */
export function useRouteSecurity() {
  // no-op
}
